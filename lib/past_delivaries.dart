import 'package:flutter/material.dart';

class PastDelivaries extends StatelessWidget {
  const PastDelivaries({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Past Delivaries"),
      ),
      
    );
  }
}